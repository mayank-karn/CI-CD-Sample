_schema-version: "3.2"
ID: cicdsample
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: cicdsample-destination-content
  type: com.sap.application.content
  requires:
  - name: cicdsample-destination-service
    parameters:
      content-target: true
  - name: cicdsample-repo-host
    parameters:
      service-key:
        name: cicdsample-repo-host-key
  - name: cicdsample-uaa
    parameters:
      service-key:
        name: cicdsample-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: cicdsample_html_repo_host
          ServiceInstanceName: cicdsample-html5-service
          ServiceKeyName: cicdsample-repo-host-key
          sap.cloud.service: cicdsample
        - Authentication: OAuth2UserTokenExchange
          Name: cicdsample_uaa
          ServiceInstanceName: cicdsample-xsuaa-service
          ServiceKeyName: cicdsample-uaa-key
          sap.cloud.service: cicdsample
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: cicdsample-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: cicdsample-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - cicdsample.zip
      name: cicdsample
      target-path: resources/
- name: cicdsample
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: cicdsample-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: cicdsample-destination-service
    service-plan: lite
- name: cicdsample-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: cicdsample-xsuaa-service
    service-plan: application
- name: cicdsample-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: cicdsample-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
